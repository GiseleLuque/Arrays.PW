const numbers = [1, 2, 3, 4, 5];
numbers[0] = 10;
numbers[2] = 30;
console.log(numbers);

//Se reemplaza el número objetivo por el número elegido.
//Es básicamente, el primer número de la lista se reemplaza por un 10 y el tercer número por un 30.
